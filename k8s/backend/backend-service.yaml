apiVersion: v1
kind: Service
metadata:
  name: esprit-market-backend
  namespace: esprit-market
  labels:
    app: esprit-market-backend
spec:
  selector:
    app: esprit-market-backend
  ports:
    - name: http
      port: 8088
      targetPort: http
      protocol: TCP
  type: ClusterIP
